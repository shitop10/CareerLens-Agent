# CareerLens Agent 项目总结

## 概述

Agentic RAG 求职文档分析助手，支持 JD 匹配和简历诊断（MVP 阶段）。基于 Chroma + BM25 + RRF 混合检索，DashScope Qwen 作为 LLM。

原始项目参考: [GitHub - zby020203/CareerLens-Agent](https://github.com/zby020203/CareerLens-Agent)

---

## 技术栈

| 层级 | 组件 |
|------|------|
| 前端 | 原生 HTML/CSS/JS SPA（CDN 无依赖） |
| API | FastAPI + SSE streaming + CORS |
| Agent | Planner → Task Plan → Tool Routing → Reflection → LongTermMemory |
| RAG | LangChain + ChatTongyi(qwen-max) + DashScopeEmbedding(text-embedding-v4) |
| 检索 | Chroma（稠密向量）+ rank-BM25（稀疏）+ 自实现 RRF 倒数秩融合 |
| 存储 | SQLite(aiosqlite async) + Chroma PersistentClient + pickle(BM25) + JSONL(记忆) |
| 文档解析 | SemanticChunker 语义分块 + PyPDF |
| LLM | 阿里云 DashScope Qwen（qwen-max + text-embedding-v4） |

---

## 项目结构

```
E:\CareerLens Agent\
├── app/
│   ├── api/
│   │   └── api_service.py          # FastAPI: 注册/登录/会话/上传/聊天流(SSE)
│   ├── core/
│   │   ├── agent.py                # Agent 引擎: 意图识别→工具选择→反思→记忆
│   │   ├── config_data.py          # 全部配置 (Chroma路径/阈值/DB URL)
│   │   ├── knowledge_base.py       # 文档上传/解析/分块/Chroma向量存储
│   │   ├── logger.py               # 日志 (RotatingFileHandler)
│   │   ├── prompts.py              # RAG提示词/标题生成/摘要生成
│   │   ├── rag.py                  # LangChain RAG链
│   │   └── vector_stores.py        # Chroma+BM25+RRF混合检索服务
│   ├── models/
│   │   └── models.py               # User/ChatSession/ChatMessage (SQLAlchemy)
│   └── utils/
│       └── file_history_store.py   # LangChain 消息历史适配器
├── data/                           # 示例数据 (JD/简历/题库等5个文件)
├── html/
│   └── index.html                  # 前端 SPA (登录/上传/分析)
├── database/                       # 运行时生成 (Chroma + SQLite + BM25 pkl)
├── logs/                           # 运行时日志
├── requirements.txt                # Python 依赖
├── .env                            # DASHSCOPE_API_KEY (不提交Git)
└── .gitignore
```

---

## 核心流程

```
用户上传文档(txt/md/pdf)
       │
       ▼
文本解析 + 语义分块 (SemanticChunker)
       │
       ▼
DashScope Embedding 向量化
       │
       ▼
存入 Chroma + 更新 BM25 语料
       
═══════════════════════════════════

用户发起分析请求
       │
       ▼
Agent 意图识别 (岗位匹配 / 简历诊断 / 综合)
       │
       ▼
Agent 生成任务计划 (6步)
       │
       ▼
工具路由:
  ├── memory_lookup (长期记忆)
  ├── hybrid_retrieval (Chroma+BM25+RRF)
  ├── job_match / resume_diagnosis (专项分析)
       │
       ▼
混合检索: Chroma(k=3) + BM25(k=3) → RRF融合 → Top3证据
       │
       ▼
证据注入 Qwen-max → SSE流式生成
       │
       ▼
Reflection 校验 (证据覆盖度检查)
       │
       ▼
长期记忆更新 + 消息持久化到 SQLite
```

---

## Agent 设计

### 意图识别 (_detect_mode)
- 匹配 "JD/岗位/职位/匹配" → **岗位匹配**
- 匹配 "简历/表达/项目经历" → **简历诊断**
- 其他 → **综合求职分析**

### 五个工具

| 工具 | 功能 |
|------|------|
| `memory_lookup` | 读取历史偏好和待追踪短板 |
| `hybrid_retrieval` | Chroma + BM25 + RRF 混合检索召回证据 |
| `job_match` | 抽取岗位能力项，输出匹配矩阵 (具备/补强/缺证据) |
| `resume_diagnosis` | 检查简历是否含问题定义/技术方案/指标结果/个人贡献/复盘 |
| (interview_coach) | 暂未启用，架构预留 |

### 反思规则
- 有证据且无失败工具 → "已召回知识库证据，可以给出具体结论"
- 无证据 → "知识库证据不足，提醒用户补充材料"
- 有工具失败 → "明确标注不确定性"

---

## 混合检索详解

```
用户 Query "大模型算法岗 RAG 能力要求"
       │
       ├──→ Chroma 稠密检索 (k=3)
       │     └─ 语义匹配: "检索增强生成" ≈ "RAG"
       │
       ├──→ BM25 稀疏检索 (k=3)
       │     └─ 关键词命中: "RAG", "Embedding", "BM25"
       │
       ▼
  RRF 倒数秩融合: score += 1/(rank + 60)
       │
       ▼
  归一化筛选 (≥97% 相关性) → Top 3 证据文档
```

---

## API 接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/health` | 健康检查 |
| POST | `/auth/register` | 注册 |
| POST | `/auth/login` | 登录 (Set-Cookie) |
| POST | `/sessions` | 创建新会话 |
| GET | `/sessions` | 获取会话列表 |
| POST | `/chat` | 发送消息 (SSE流式) |
| GET | `/chat/{uuid}` | 获取历史消息 |
| POST | `/knowledge/upload` | 上传文档 (txt/md/pdf) |
| GET | `/agent/memory` | 查询长期记忆 |
| DELETE | `/delete/{uuid}` | 删除会话 |

---

## 启动方式

```bash
# 1. 安装依赖
pip install -r requirements.txt

# 2. 配置 API Key (二选一)
# 方式A: 创建 .env 文件 (推荐)
echo "DASHSCOPE_API_KEY=你的阿里云百炼API密钥" > .env

# 方式B: 设置环境变量
export DASHSCOPE_API_KEY="你的阿里云百炼API密钥"

# 3. 启动服务
python -m app.api.api_service

# 4. 浏览器打开
# http://localhost:8000/html/index.html
```

---

## 与原始项目的差异

| 项目 | 原始 (zby020203) | MVP (当前) |
|------|-----------------|-----------|
| 向量数据库 | Milvus Lite | **Chroma** (免Docker) |
| 模拟面试 | 支持 | 暂未实现 (架构预留) |
| uvloop | 依赖 | 移除 (Windows 不兼容) |
| 前端框架 | 声称 Vue 3，实际原生 JS | 原生 HTML/CSS/JS |
| 上传方式 | FastAPI + Streamlit | FastAPI (浏览器内上传) |
| .env 加载 | 手动 export | python-dotenv 自动加载 |

---

## 数据库模型

```
User (用户)
  ├── id, username, hashed_password, last_cookie
  └── sessions → ChatSession[]

ChatSession (会话)
  ├── id, session_uuid, title
  └── messages → ChatMessage[]

ChatMessage (消息)
  ├── id, user_input, raw_output
  ├── output_uncode (纯文本), code (代码块)
  └── streamline_input (60字摘要用于上下文压缩)
```

---

## 版本信息

- Python: 3.13
- 创建日期: 2026-07-13
- 状态: MVP 可用，JD 匹配 + 简历诊断功能已跑通
