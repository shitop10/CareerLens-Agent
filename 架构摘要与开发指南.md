# CareerLens Agent — 架构摘要与开发指南

> 生成时间：2026-08-26 ｜ 面向：后续开发 ｜ 依据：全量源码精读（app/ 15 个 .py + 前端 SPA + 参考源副本对比）

---

## 1. 项目概览

**一句话定位**：CareerLens 是一个面向大模型算法岗求职者的 **Agentic RAG 求职文档分析助手** —— 用户上传 JD / 简历 / 项目素材 / 面试题库后，系统通过「Agent 四阶段闭环（Planner → Tool Routing → Reflection → LongTermMemory）+ 混合检索（Chroma + BM25 + RRF）」自动生成岗位匹配、简历诊断等求职建议。

**当前状态**：MVP 可用（JD 匹配 + 简历诊断已跑通），模拟面试为架构预留、尚未启用。

| 维度 | 现状 |
|------|------|
| 代码规模 | 后端 ~15 个 Python 文件，整体 ~1500 行 |
| API 端点 | 13 个（FastAPI） |
| Agent 工具 | 4 个（memory_lookup / hybrid_retrieval / job_match / resume_diagnosis） |
| 检索管线 | Chroma 稠密(k=3) + BM25 稀疏(k=3) → RRF 融合(k=60, ≥97% 过滤) → Top3 |
| LLM | DashScope Qwen：qwen-max(对话) / qwen-turbo(标题+摘要) / text-embedding-v4(向量) |
| 存储 | SQLite(aiosqlite) + Chroma PersistentClient + BM25 pickle + JSONL 记忆 |
| 前端 | 原生 HTML/CSS/JS 单文件 SPA（零框架，CDN 无依赖） |
| 部署 | 免 Docker，`python -m app.api.api_service` 一键启动 |

---

## 2. 技术栈分层

| 层级 | 组件 | 说明 |
|------|------|------|
| 前端 | 原生 SPA（html/index.html） | 登录/上传/多会话聊天，SSE 流式渲染 |
| API | FastAPI 0.135 + uvicorn + CORS + StaticFiles | 同步服务经 `asyncio.to_thread` 桥接 |
| Agent | 自研 `CareerAgentService`（确定性规则） | 意图识别→计划→工具路由→反思→记忆 |
| RAG | LangChain 1.2 LCEL Chain + ChatTongyi | context+history+input → prompt → qwen-max → StrOutputParser |
| 检索 | Chroma + rank-BM25 + 自实现 RRF | PersistentClient 持久化，hnsw cosine |
| 解析 | SemanticChunker（语义分块, percentile）+ pypdf | txt/md/pdf |
| 存储 | SQLAlchemy 2.0 async + aiosqlite；JSONL；pickle | 用户/会话/消息；长期记忆；BM25 语料 |

---

## 3. 目录结构与职责

```
E:\CareerLens Agent\
├── app/                          ★ 当前活跃代码（开发以此为准）
│   ├── api/api_service.py        FastAPI 全部端点 + SSE 聊天流 + 上传解析 + 异步存储
│   ├── core/
│   │   ├── agent.py              Agent 引擎：CareerAgentService + LongTermMemory
│   │   ├── config_data.py        全部配置（路径/模型/阈值/DB URL，dotenv 加载 .env）
│   │   ├── knowledge_base.py     文档解析/语义分块/向量入库/BM25 语料维护/MD5 去重
│   │   ├── logger.py             日志（RotatingFileHandler，10MB×5，logs/rag_system.log）
│   │   ├── prompts.py            RAG 系统提示词 / 标题生成 / 摘要生成
│   │   ├── rag.py                RagService：LangChain 链组装 + 消息历史
│   │   └── vector_stores.py      Chroma+BM25+RRF 混合检索服务
│   ├── models/models.py          User / ChatSession / ChatMessage（SQLAlchemy 2.0 Mapped）
│   └── utils/file_history_store.py  LangChain BaseChatMessageHistory 适配器（读 SQLite）
├── html/index.html               前端 SPA（登录/上传/岗位匹配/简历诊断）
├── data/                         5 个示例资料（JD 样例/简历素材/题库/表达规则/答辩速记）
├── database/                     运行时生成：chroma_db/ career_chat.db / career_bm25_corpus.pkl /
│                                 career_agent_memory.jsonl / career_md5.text
├── logs/                         运行时日志
├── requirements.txt              依赖（含大量未使用的重型包，见 §10 技术债）
├── CareerLens-Agent-source/      原始参考仓库副本（Milvus 版，含 evaluation/ 与 Streamlit 上传）
├── CareerLens-Agent-source-ssh/  与 source 内容一致的备份副本（仅 .git 内部不同）
├── 说明文档.txt / 项目总结.md / 面试项目介绍指南.md / 项目问答总结.md   背景与面试资料
```

> **重要**：`CareerLens-Agent-source` 与 `-source-ssh` 是同一项目的旧版副本（内部仍在调用 `search_milvus`、含 Streamlit 上传器）。日常开发**只改 `app/`**；评估模块（evaluation/）目前只存在于副本中，未随 Chroma 迁移（见 §10.1）。

---

## 4. 系统架构分层

```
┌────────────────────────────────────────────────────────────┐
│ 前端 SPA (html/index.html)                                  │
│  登录/注册 → 上传资料 → 模式切换 → SSE 流式聊天              │
└────────────────────────┬───────────────────────────────────┘
                         │ HTTP / SSE (text/plain)
┌────────────────────────▼───────────────────────────────────┐
│ FastAPI (api_service.py) — 13 端点                          │
│  /auth/*  /sessions  /chat(SSE)  /knowledge/upload          │
│  /agent/memory  /health  /delete/{uuid}                     │
└────────────────────────┬───────────────────────────────────┘
                         │
          ┌──────────────┴──────────────┐
          ▼                             ▼
┌────────────────────┐      ┌───────────────────────────────┐
│ CareerAgentService  │      │ RagService (LCEL Chain)       │
│ (agent.py)          │      │ context+history+input         │
│ 意图识别 _detect_mode│─────▶│ → rag_prompt → ChatTongyi     │
│ 计划 _build_plan     │ 增强 │ → StrOutputParser (SSE)       │
│ 工具路由 _select_tools│ 输入 │                               │
│ 反思 _reflect        │      └──────────────┬────────────────┘
│ 记忆 finalize        │                     │
└──────────┬───────────┘                     │ hybrid_search_workflow
           │                                 ▼
           │                        ┌────────────────────┐
           │                        │ VectorStoreService  │
           │                        │ Chroma(k=3)+BM25(k=3)│
           │                        │ → RRF(k=60) → Top3   │
           │                        └────────────────────┘
           ▼
┌────────────────────────────────────────────────────────────┐
│ 存储层                                                      │
│ SQLite(用户/会话/消息)  Chroma(向量)  pickle(BM25)  JSONL(记忆)│
└────────────────────────────────────────────────────────────┘
```

---

## 5. 核心数据流

### 5.1 文档入库流（`/knowledge/upload` → `KnowledgeBaseService.upload_by_str`）

```
上传文件(txt/md/pdf)
  → extract_upload_text：多编码尝试(gb18030兜底) / pypdf 解析
  → MD5 查重（database/career_md5.text，命中则跳过）
  → SemanticChunker 语义分块（DashScope embedding 计算断点, percentile, buffer=1）
  → embed_documents → Chroma get_or_create_collection(hnsw:space=cosine) 批量写入
  → chunks 追加到 bm25_corpus → pickle 落盘
  → 记录 MD5
```

### 5.2 问答流（`POST /chat` → SSE）

```
input_text
  → CareerAgentService.prepare()  [asyncio.to_thread]
      _detect_mode 关键词判定：简历→简历诊断 / JD·岗位→岗位匹配 / 其他→综合
      _build_plan  6 步计划（mode 相关）
      _select_tools  memory_lookup + hybrid_retrieval (+ job_match / resume_diagnosis)
      _reflect      依据 doc_count 与 error 标记给出反思结论
  → 状态行逐条 yield（每行 sleep 0.08s 模拟流式进度）
  → compose_agent_context() 把「计划+工具结果+反思」打包 → 追加到用户输入 → enhanced_input
  → RagService.chain.astream()（RunnableWithMessageHistory, session_id=session_uuid）
      ├─ retrieve_context → hybrid_search_workflow → 状态 print 被 redirect_stdout 捕获转发
      └─ qwen-max 流式 token → 逐 chunk yield
  → finalize() 规则提取长期记忆 → JSONL 追加（去重）
  → asyncio.create_task(save_chat_history)：生成标题(qwen-turbo)、60字摘要、存 ChatMessage
```

---

## 6. 核心模块源码要点

### 6.1 agent.py — 确定性 Agent 引擎
- **设计范式**：Plan-and-Execute（非 ReAct）。意图识别用**关键词规则**而非 LLM（零延迟、零成本）。
- `_detect_mode` 判定顺序：**简历相关词优先于 JD 词**（`简历/表达/项目经历/经历` → 简历诊断；`JD/岗位/职位/匹配/要求`+`job` → 岗位匹配；否则综合）。
- 工具执行**串行 for 循环**，异常捕获后以 `metadata={"error": True}` 标记失败，不影响整体流程（容错）。
- `_reflect` 三态：工具失败→"标注不确定性"；有证据(doc_count>0)→"给出具体结论"；无证据→"提醒补充材料"。
- `LongTermMemory`：JSONL 追加写；`_extract_memory_candidates` 用关键词规则提取三类记忆（技术标签/待补强项/简历表达偏好）；加载时去重（最近 40 条比对）。**刻意不用 LLM/向量做记忆**（MVP 取舍）。
- `job_match` / `resume_diagnosis` 工具本质是**规则化提示词注入**，真正的分析由 LLM 在生成阶段完成。

### 6.2 rag.py — LangChain RAG 链
- LCEL：`{context, input, history} | prompt | ChatTongyi(qwen-max) | StrOutputParser`。
- `RunnableWithMessageHistory` 挂 `get_history(session_id)` 读取 SQLite 历史。
- 检索上下文通过 `RunnableLambda(retrieve_context)` 在链内完成，内部 `print` 状态消息（供外层 SSE 捕获）。

### 6.3 vector_stores.py — 混合检索
- `search_chroma`：embed_query → collection.query(n_results=k) → Document(filename, score=1-distance)。
- `get_retriever`：稠密（自定义 ChromaRetriever）+ 稀疏（BM25Retriever，k=3）；若 BM25 语料缺失则退化为纯稠密。
- **RRFRetriever**：`score += 1/(rank+60)` → 归一化（最高分=100%）→ 过滤 ≥97% → 最多 Top3。
- `hybrid_search_workflow` 返回 `(status_messages, results)`，状态消息供前端展示检索过程。

### 6.4 knowledge_base.py — 入库与材料管理
- 基于 `langchain_experimental.SemanticChunker`（DashScopeEmbeddings 计算句向量断点）。
- Chroma metadata 含 `filename / chunk_index / active / uploaded_at`；检索只召回 `active=True` 的文档（"切换材料"机制，startup 惰性迁移为旧 chunks 补 active）。
- 材料管理：`list_files()`（按文件聚合）、`toggle_file()`（启用/停用，**替换式更新 metadata**）、`delete_file()`（删 chunks + 清理 MD5 + **重建 BM25**——pkl 从 Chroma active 文档全量重建，与检索保持一致）。
- MD5 去重记录格式 `filename|md5`（兼容旧纯 md5 行）；删除材料后重新上传不再被跳过。

### 6.5 models.py + file_history_store.py — 存储与历史
- `User(last_cookie)` 存登录 uuid；`ChatSession(session_uuid)`；`ChatMessage(user_input / raw_output / output_uncode / code / streamline_input)`。
- `DatabaseChatMessageHistory`：**add_messages 为空实现**（pass）；历史写入完全依赖异步 `save_chat_history`；读取时旧消息用 `streamline_input`（60 字摘要）压缩上下文、最新一条用全文。
- 同步 engine 与异步 engine 并存（历史读取用同步 SQLAlchemy）。

### 6.6 api_service.py — 13 端点（详见 §7）
- 服务单例懒加载（`get_rag_service` / `get_agent_service` / `get_kb_service`）。
- `/chat` 为**结构化 SSE 事件流**（`text/event-stream`）：`sse_event()` 统一格式化事件帧；`reasoning` 事件（status/plan/tool/reflection/memory 子类型，来自 `AgentRun.reasoning_events()`）与 `answer` 事件（LLM token）分离，`done` 收尾；检索状态 print 仍经 `redirect_stdout` 捕获，但归入 reasoning/status 事件（详见 `docs/plans/2026-08-26-agent-output-separation-design.md`）。
- 登录：`sha256(password + SALT)`，`Set-Cookie: session_id`（httponly, samesite=lax, secure=False）。
- `save_chat_history`：首条消息时用 qwen-turbo 生成标题；每条生成 60 字摘要；代码块正则抽取。

### 6.7 html/index.html — 前端
- 单文件 SPA：`loginScreen` / `appShell`（sidebar + workspace 双栏）。
- 三种模式：`upload`（拖拽/选择 + 写入知识库）、`match`（岗位匹配）、`resume`（简历诊断），每种带快捷提问。
- 聊天用 `fetch` + `ReadableStream` 读 SSE，`bubble.textContent = fullText` 增量渲染（无 Markdown 渲染、无引用高亮）。
- 前端预填测试账号 `candidate01 / career123`。

---

## 7. API 端点清单

| 方法 | 路径 | 鉴权 | 说明 |
|------|------|------|------|
| GET | `/health` | 无 | 健康检查 |
| POST | `/auth/register` | 无 | 注册（query 参数传 username/password） |
| POST | `/auth/login` | 无 | 登录，Set-Cookie session_id |
| POST | `/sessions` | Cookie | 创建会话 |
| GET | `/sessions` | Cookie | 会话列表（按 update_time 倒序） |
| POST | `/chat` | Cookie | 发送消息，SSE 结构化流（body: session_uuid, input_text；事件：reasoning/answer/done） |
| GET | `/chat/{uuid}` | **无校验** | 获取某会话历史消息 |
| POST | `/knowledge/upload` | Cookie | multipart 上传 txt/md/pdf |
| GET | `/knowledge/files` | Cookie | 材料列表（按 filename 聚合：chunks / active / 上传时间） |
| POST | `/knowledge/files/toggle` | Cookie | 切换材料启用/停用（body: filename），并重建 BM25 |
| DELETE | `/knowledge/files?filename=` | Cookie | 删除材料（Chroma + MD5 清理 + 重建 BM25） |
| GET | `/agent/memory` | Cookie | 读取长期记忆（最近 12 条） |
| DELETE | `/delete/{uuid}` | **无校验** | 删除会话 |

> ⚠️ `/chat/{uuid}` 与 `/delete/{uuid}` **未校验会话归属**（甚至未要求登录），属安全隐患，见 §10。

---

## 8. 关键技术决策（面试/评审要点）

| 决策 | 动机 |
|------|------|
| Chroma 替换 Milvus Lite | 免 Docker、纯 Python、Windows 友好，降低部署门槛；代价是大规模性能 |
| 混合检索 + RRF（k=60） | 向量擅语义、BM25 擅精确术语，二者互补；RRF 只依赖排名，跨检索器可比，避免量纲/超参问题 |
| ≥97% 归一化过滤 | 以最高分为基准过滤弱相关证据，保 Top3 质量 |
| 意图识别用关键词不用 LLM | 确定性、零延迟、零成本（求职场景模式仅 3 类，足够） |
| Plan-and-Execute 而非 ReAct | 求职分析步骤固定，批量规划省 Token、执行快、可控 |
| 记忆用 JSONL+规则而非向量 | 记忆需求结构化简单，MVP 阶段避免过度设计 |
| 语义分块 SemanticChunker | 相比固定长度分块，chunk 语义完整，召回片段更有用 |
| 前后端同源免构建 | 原生 SPA + StaticFiles 挂载，一键启动演示 |

---

## 9. 已知问题与技术债（开发前必读）

### 9.1 评估模块失效（高优先级）
`CareerLens-Agent-source/app/evaluation/evaluator.py` 仍调用 `self.vector_service.search_milvus(...)`，而当前 `VectorStoreService` 只有 `search_chroma`。**该评估代码在当前版本无法运行**（AttributeError），且 `app/` 下未包含 evaluation 模块、`run_evaluation.py` 也不存在。项目总结中"架构预留评估模块"实际**未落地**。

### 9.2 潜在 Bug / 健壮性问题
1. **Chroma id 冲突**：`doc_{existing_count+i}` 在「同一文件重复上传被 MD5 跳过以外的场景（如删除重建/并发上传）」可能产生 id 偏移或冲突；且 collection.add 用自增计数，多线程并发上传存在竞态。
2. **`add_messages` 空实现**：LangChain 历史回写缺失，历史完全依赖 `asyncio.create_task(save_chat_history)` 异步落库；若请求结束立即刷新/切换会话，最后一条消息可能尚未写入。
3. ~~SSE 状态转发 hack~~ **已于 2026-08-26 解决**：`/chat` 协议升级为标准 `text/event-stream` 事件流（reasoning/answer/done），思考过程与最终答案在协议层分离、前端折叠面板分开展示（见 `docs/plans/2026-08-26-agent-output-separation-design.md` 与对应实现计划）。`redirect_stdout` 仅保留用于捕获检索状态并归入 reasoning 事件。
4. **配置死参数**：`DENSE_WEIGHT=0.7 / SPARSE_WEIGHT=0.3`、`MAX_SPLIT_CHAR_NUMBER=1000` 定义了但未在任何地方使用（RRF 是排名融合、SemanticChunker 未设长度上限）。
5. ~~前端状态不持久化~~ **已于 2026-08-26 解决（问题1+问题3）**：会话切换会调用 `GET /chat/{uuid}` 回显历史消息；材料列表改为真实接口 `GET /knowledge/files`（刷新后仍在），并支持启用/停用切换与删除（详见 `docs/plans/2026-08-26-session-history-fix-design.md`、`docs/plans/2026-08-26-knowledge-file-management-design.md`）。
6. **面试能力缺口**：模拟面试（interview_coach 工具）在 source 版存在、当前版已删除；前端也无 mock_interview 模式入口——与 README/面试材料描述不符。

### 9.3 安全
1. ~~`/chat/{uuid}`、`/delete/{uuid}` 无鉴权~~ **部分解决（2026-08-26）**：`DELETE /delete/{uuid}` 已加 Cookie 鉴权与归属校验（401/403/404）；`GET /chat/{uuid}` 仍有待补鉴权（低风险，uuid 不可枚举）。
2. 密码用 `sha256+salt`（弱哈希，建议 bcrypt/argon2）；Cookie 无过期时间、secure=False。
3. `CORS allow_origins=["*"]` + `allow_credentials=True` 组合不安全（本地单机演示可接受，上线前必须收紧）。

### 9.4 依赖过重
`requirements.txt` 含 torch / transformers / sentence-transformers / FlagEmbedding / peft / accelerate 等大量与当前功能无关的重型包（可能是评估或备用 Embedding 遗留），安装体积大、耗时长。建议拆分为 `requirements-core.txt`。

---

## 10. 开发路线图建议（按优先级）

| 优先级 | 任务 | 说明 |
|--------|------|------|
| P0 | 修复并迁入评估模块 | 把 evaluator 改为 `search_chroma`，迁入 `app/evaluation/`，补 `run_evaluation.py`，跑通 Hit Rate / MRR / Faithfulness / Relevancy 基线 |
| P0 | 鉴权与安全加固 | `/chat/{uuid}` `/delete/{uuid}` 增加 Cookie 与归属校验；密码改 bcrypt；CORS 收紧 |
| P1 | 恢复模拟面试 | 从 source 版恢复 `interview_coach` 工具 + `_detect_mode` 面试分支 + 前端第 4 个模式页 |
| P1 | Chroma id 生成重构 | 用内容哈希（如 md5 前 12 位）替代自增计数，消除重复/并发冲突 |
| P1 | 历史消息回显 | 前端切换会话时调用 `GET /chat/{uuid}` 渲染历史；修复"刷新丢失"体验 |
| P2 | LangGraph 重写 Agent | 用 StateGraph 表达 plan→route→reflect 循环，支持条件边/循环（面试亮点，参考指南 §2） |
| P2 | Reranker 二次排序 | 引入 BGE-Reranker / Qwen3-Reranker 对 RRF TopK 重排，提升精确率 |
| P2 | 证据引用高亮 | 回答中标注 `[来源: filename]` 并前端高亮，增强可溯源（幻觉控制闭环） |
| P2 | 多格式支持 | docx / 图片 OCR（配合 TextIn 等解析服务） |
| P3 | 依赖精简 | 拆分 core/ 重型依赖，缩短安装时间 |

> ✅ 已落地（2026-08-26）：Agent 输出「思考与答案分离展示」（P1 级改造，见 `docs/plans/2026-08-26-agent-output-separation-design.md`）。

---

## 11. 本地开发指南

```bash
# 1. 安装依赖（建议虚拟环境）
pip install -r requirements.txt

# 2. 配置 API Key（二选一）
#   方式A：项目根目录创建 .env
echo "DASHSCOPE_API_KEY=你的阿里云百炼密钥" > .env
#   方式B：环境变量
export DASHSCOPE_API_KEY="你的阿里云百炼密钥"

# 3. 启动服务（根目录执行）
python -m app.api.api_service
#    等价于 uvicorn app.api.api_service:app --host 0.0.0.0 --port 8000

# 4. 访问前端
#    http://localhost:8000/html/index.html
#    测试账号：candidate01 / career123（前端已预填）

# 5. 先灌入示例数据（可选，data/ 下 5 个文件，前端上传或脚本调用）
#    知识库为空时，Agent 反思会提示"证据不足，请补充材料"
```

**目录归属提醒**：`database/`、`logs/` 为运行时生成；改代码只动 `app/`、`html/`。三个源码副本中只有 `app/` 是活跃代码，另两个为历史备份（source 含评估与 Streamlit 上传器，可作为功能恢复参考）。

---

## 12. 附：模块 → 文件速查

| 想改什么 | 改哪里 |
|----------|--------|
| 新增 API 端点 | `app/api/api_service.py` |
| 新增 Agent 工具 / 模式 | `app/core/agent.py`（tools 字典 + _detect_mode + _select_tools） |
| 调整提示词 | `app/core/prompts.py` |
| 换模型 / 调参数 | `app/core/config_data.py`（路径、模型名、k、阈值） |
| 检索策略 | `app/core/vector_stores.py` |
| 入库 / 分块 | `app/core/knowledge_base.py` |
| 数据模型 | `app/models/models.py` |
| 前端页面 | `html/index.html` |
| 数据库结构变更 | `models.py` + 删除 `database/career_chat.db` 重建（MVP 无迁移工具） |
