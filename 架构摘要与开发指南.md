# CareerLens Agent — 架构摘要与开发指南

> 生成时间：2026-08-26 ｜ **最近更新：2026-09-10（双 Planner 架构 + 对话历史）**
> 面向：后续开发 ｜ 依据：全量源码精读（app/ 16 个实现模块 + 前端 SPA + 测试 + 参考源副本对比）

---

## 0. 本次更新摘要（2026-09-10）

| 变更 | 说明 | 文档 |
|---|---|---|
| **双 Planner 架构** | 新增 LLM function calling 规划路线，与原有规则路线并存、运行时可切换 | `docs/plans/2026-09-10-dual-planner-design.md` |
| **Planner A/B 评测** | 20 条标注用例 + 指标对比 + 报告生成，P0 技术债「评估模块失效」已闭环 | 同上 §11、`docs/plans/2026-09-10-dual-planner-eval.md` |
| **规划器接入对话历史** | 省略式追问（「那这个呢」）可正确解析指代 | 同上 §12 |
| **落库竞态修复** | `persist_message` 提到 `done` 之前，消除"立刻追问读不到上一轮" | 同上 §12.3 |
| **前端诚实标注** | 规则路线明确标注「无模型推理」，不再伪装成模型思考轨迹 | 同上 §2（G4） |
| **测试体系** | 0 → 214 项测试（14 个模块），含零网络桩测试与前端契约测试 | `run_tests.py` |

**对外表述红线**：规则路线的 `plan` / `reflection` 是硬编码产出，**不是模型推理**。
所有对外展示必须携带 `planner="rule"` 标记，不得表述为「LLM 自主规划」。

---

## 1. 项目概览

**一句话定位**：CareerLens 是一个面向大模型算法岗求职者的 **Agentic RAG 求职文档分析助手** —— 用户上传 JD / 简历 / 项目素材 / 面试题库后，系统通过「**双 Planner 编排（规则路由 / LLM function calling）+ 混合检索（Chroma + BM25 + RRF）**」自动生成岗位匹配、简历诊断等求职建议。

**当前状态**：MVP 可用（JD 匹配 + 简历诊断已跑通并完成 A/B 评测），模拟面试为架构预留、尚未启用。

| 维度 | 现状 |
|------|------|
| 代码规模 | `app/` 下 16 个实现模块，其中本轮新增 6 个文件（约 1.7k 行） |
| API 端点 | 13 个（FastAPI，本次无新增端点，`/chat` 增参、`/health` 扩字段） |
| Planner 路线 | 2 条：`rule`（确定性关键词路由，默认） / `llm`（LangGraph + function calling） |
| Agent 工具 | 4 个（memory_lookup / hybrid_retrieval / job_match / resume_diagnosis），**两条路线共用唯一实现** |
| 检索管线 | Chroma 稠密(k=3) + BM25 稀疏(k=3) → RRF 融合(k=60, ≥97% 过滤) → Top3 |
| LLM | DashScope Qwen：qwen-max(规划+对话) / qwen-turbo(反思+标题+摘要) / text-embedding-v4(向量) |
| 会话上下文 | 最近 3 轮 × 每侧 80 字压缩摘要注入规划 prompt |
| 存储 | SQLite(aiosqlite) + Chroma PersistentClient + BM25 pickle + JSONL 记忆 |
| 前端 | 原生 HTML/CSS/JS 单文件 SPA（零框架，含 Planner 切换与开销量化展示） |
| 测试 | **214 项 / 14 个模块**，`python run_tests.py` 一键全跑（默认零网络） |
| 评测 | `python run_planner_eval.py` 产出 A/B 指标表与 markdown 报告 |
| 部署 | 免 Docker，`python -m app.api.api_service` 一键启动 |

---

## 2. 技术栈分层

| 层级 | 组件 | 说明 |
|------|------|------|
| 前端 | 原生 SPA（html/index.html） | 登录/上传/多会话聊天，SSE 流式渲染，Planner 切换 |
| API | FastAPI 0.135 + uvicorn + CORS + StaticFiles | 同步服务经 `asyncio.to_thread` 桥接 |
| **规划层** | `BaseCareerAgent` → `RulePlannerAgent` / `LlmPlannerAgent` | 两条路线产出同一 `AgentRun` 契约 |
| **编排（LLM 路线）** | LangGraph 1.1 `StateGraph` | `plan → execute → (plan) → reflect` 状态机 |
| **工具层** | `ToolRegistry`（`app/core/tools.py`） | 4 个工具唯一实现；同时导出 `invoke()` 与 LangChain `BaseTool` 视图 |
| RAG | LangChain 1.2 LCEL Chain + ChatTongyi | context+history+input → prompt → qwen-max → StrOutputParser |
| 检索 | Chroma + rank-BM25 + 自实现 RRF | PersistentClient 持久化，hnsw cosine |
| 解析 | SemanticChunker（语义分块, percentile）+ pypdf | txt/md/pdf |
| 存储 | SQLAlchemy 2.0 async + aiosqlite；JSONL；pickle | 用户/会话/消息；长期记忆；BM25 语料 |
| 评测 | 自研 `app/evaluation/`（纯函数指标 + 双路线评测器） | 无第三方评测框架依赖 |

---

## 3. 目录结构与职责

```
E:\CareerLens Agent\
├── app/                          ★ 当前活跃代码（开发以此为准）
│   ├── api/api_service.py        FastAPI 全部端点 + SSE 聊天流 + 上传解析 + 消息落库
│   ├── core/
│   │   ├── agent_types.py        【新】共享契约：AgentToolResult / AgentRun / planner 常量
│   │   │                               / token 用量提取 / SSE 事件构造
│   │   ├── tools.py              【新】ToolRegistry：4 个工具唯一实现 + LangChain 工具视图
│   │   ├── agent_base.py         【新】LongTermMemory / BaseCareerAgent / 历史摘要
│   │   │                               / rule_based_reflection / build_agent_service 工厂
│   │   ├── agent.py              【重构】RulePlannerAgent（原 CareerAgentService）
│   │   ├── llm_agent.py          【新】LlmPlannerAgent：LangGraph plan→execute→reflect
│   │   ├── config_data.py        全部配置（路径/模型/阈值/DB URL/Planner 与历史参数）
│   │   ├── knowledge_base.py     文档解析/语义分块/向量入库/BM25 语料维护/MD5 去重
│   │   ├── logger.py             日志（RotatingFileHandler，10MB×5，logs/rag_system.log）
│   │   ├── prompts.py            RAG 提示词 / Planner / Reflector / Judge / 标题 / 摘要
│   │   ├── rag.py                RagService：LangChain 链组装 + 消息历史
│   │   └── vector_stores.py      Chroma+BM25+RRF 混合检索服务
│   ├── evaluation/               【新】Planner A/B 评测
│   │   ├── dataset.py            20 条人工标注用例 + 数据集校验
│   │   └── evaluator.py          纯函数指标 + PlannerABEvaluator + markdown 报告
│   ├── models/models.py          User / ChatSession / ChatMessage（SQLAlchemy 2.0 Mapped）
│   └── utils/file_history_store.py  LangChain BaseChatMessageHistory 适配器（读 SQLite）
├── html/index.html               前端 SPA（登录/上传/岗位匹配/简历诊断 + Planner 切换）
├── tests/                        【新】14 个测试模块 / 214 项（默认零网络）
├── run_tests.py                  【新】测试统一入口
├── run_planner_eval.py           【新】Planner A/B 评测 CLI
├── data/                         5 个示例资料（JD 样例/简历素材/题库/表达规则/答辩速记）
├── database/                     运行时生成：chroma_db/ career_chat.db / career_bm25_corpus.pkl /
│                                 career_agent_memory.jsonl / career_md5.text
├── docs/plans/                   设计文档与实施计划（含本轮 3 份）
├── logs/                         运行时日志
├── requirements.txt              依赖（含大量未使用的重型包，见 §9.4）
├── CareerLens-Agent-source/      原始参考仓库副本（Milvus 版，含 Streamlit 上传）
├── CareerLens-Agent-source-ssh/  与 source 内容一致的备份副本
├── 说明文档.txt / 项目总结.md / 面试项目介绍指南.md / 项目问答总结.md   背景与面试资料
```

> **重要**：`CareerLens-Agent-source` 与 `-source-ssh` 是同一项目的旧版副本（内部仍在调用 `search_milvus`）。日常开发**只改 `app/`**。

---

## 4. 系统架构分层

```
┌──────────────────────────────────────────────────────────────────┐
│ 前端 SPA (html/index.html)                                        │
│  登录/注册 → 上传资料 → 模式切换 → Planner 切换 → SSE 流式聊天      │
└───────────────────────────┬──────────────────────────────────────┘
                            │ HTTP / SSE (text/event-stream)
┌───────────────────────────▼──────────────────────────────────────┐
│ FastAPI (api_service.py) — 13 端点                                │
│  /auth/*  /sessions  /chat(SSE)  /knowledge/*  /agent/memory      │
│  /health  /delete/{uuid}                                          │
└───────────────────────────┬──────────────────────────────────────┘
                            │ planner_mode 解析 → build_agent_service
              ┌─────────────┴──────────────┐
              ▼                            ▼
┌────────────────────────┐   ┌────────────────────────────────────┐
│ RulePlannerAgent       │   │ LlmPlannerAgent (LangGraph)         │
│ (agent.py)             │   │ (llm_agent.py)                      │
│  _match_intent 关键词   │   │  plan  LLM + bind_tools             │
│  _resolve_mode 历史回落 │   │  execute 过滤幻觉/去重后调工具       │
│  _build_plan 固定模板   │   │  reflect LLM 评审证据                   │
│  0 token / 可复现       │   │  失败 → 回退 RulePlannerAgent         │
└───────────┬────────────┘   └──────────────┬─────────────────────┘
            └───────────────┬───────────────┘
                            ▼
              ┌──────────────────────────────┐
              │ ToolRegistry (tools.py)       │
              │ memory_lookup / hybrid_retrieval│
              │ resume_diagnosis / job_match  │
              └──────────────┬───────────────┘
                            ▼
              ┌──────────────────────────────┐
              │ AgentRun（统一契约）           │
              │ plan / tool_results /         │
              │ reflection / metrics /        │
              │ planner / fallback_reason     │
              └──────────────┬───────────────┘
                            ▼ compose_agent_context → enhanced_input
              ┌──────────────────────────────┐
              │ RagService (LCEL Chain)       │
              │ context+history+input         │
              │ → rag_prompt → ChatTongyi     │
              │ → StrOutputParser (SSE)       │
              └──────────────┬───────────────┘
                             │ hybrid_search_workflow
                             ▼
              ┌──────────────────────────────┐
              │ VectorStoreService            │
              │ Chroma(k=3)+BM25(k=3)         │
              │ → RRF(k=60) → Top3            │
              └──────────────────────────────┘

存储层：SQLite(用户/会话/消息)  Chroma(向量)  pickle(BM25)  JSONL(记忆)
```

---

## 5. 核心数据流

### 5.1 文档入库流（`/knowledge/upload` → `KnowledgeBaseService.upload_by_str`）

```
上传文件(txt/md/pdf)
  → extract_upload_text：多编码尝试(gb18030兜底) / pypdf 解析
  → MD5 查重（database/career_md5.text，命中则跳过）
  → SemanticChunker 语义分块（DashScope embedding 计算断点, percentile, buffer=1）
  → embed_documents → Chroma get_or_create_collection(hnsw:space=cosine) 批量写入
  → chunks 追加到 bm25_corpus → pickle 落盘
  → 记录 MD5
```

### 5.2 问答流（`POST /chat` → SSE）

```
body: input_text, session_uuid, planner_mode(可选)

→ 解析 planner_mode（请求 > 配置默认 PLANNER_MODE），取缓存的 Agent 实例
→ agent.prepare(input_text, session_uuid)  [asyncio.to_thread]
    ├─ load_history(session_uuid) → 最近 3 轮 × 80 字压缩摘要
    │
    ├─ [rule] _match_intent(当前问题) → 未命中则回看历史用户发言 → _resolve_mode
    │         _build_plan 固定模板 / tools.invoke 串行执行 / _reflect 三态规则反思
    │         0 token；metrics 全 0
    │
    └─ [llm]  LangGraph.invoke
               plan   : ChatTongyi(qwen-max).bind_tools(4 工具)
                        human 消息 = 历史摘要 + 当前问题
               execute: 丢弃幻觉工具名、跳过同参重复调用、串行执行注册表工具
               plan   : 有条件回边（最多 MAX_TOOL_ROUNDS 轮执行）
               reflect: ChatTongyi(qwen-turbo) 评审证据；失败退回规则反思
               任一步异常或全部工具幻觉 → 回退 RulePlannerAgent 并写 fallback_reason
               回退时保留本次尝试的 token 与调用次数（账目真实）
    → AgentRun(planner, plan, tool_results, reflection, metrics, fallback_reason)

→ SSE event:reasoning  {type:"planner",  planner, label, note}      ← 首帧显式声明路线
→ SSE event:reasoning  AgentRun.reasoning_events()
     status → plan → tool → reflection（每个事件带 planner 字段）
→ compose_agent_context() 打包「计划+工具结果+反思」→ 追加到用户输入 → enhanced_input
→ RagService.chain.astream()（RunnableWithMessageHistory, session_id=session_uuid）
    ├─ retrieve_context → hybrid_search_workflow → 状态 print 经 redirect_stdout 捕获转发
    └─ qwen-max 流式 token → 逐 chunk 产出 event:answer
→ finalize() 规则提取长期记忆 → JSONL 追加（去重）→ event:reasoning {type:"memory"}
→ SSE event:reasoning  {type:"metrics", 规划耗时/token/轮次/工具数/历史轮次}
→ await persist_message()   ← ★ 必须在 done 之前，否则下一轮读不到历史
→ SSE event:done
→ asyncio.create_task(enrich_message())  后台生成标题与摘要并回写
```

> **为什么落库要拆两步**：早先 `save_chat_history` 是流结束后的后台任务，且先生成标题+摘要
> （约 1 秒）才写消息行。用户收到回答后立刻追问时，下一轮规划器读不到上一轮 → 指代消解失效。
> 现在 `persist_message`（毫秒级写入，摘要先占位）在 `done` 前 await，
> `enrich_message`（慢）转为后台补写。详见设计文档 §12.3。

### 5.3 SSE 事件类型总表

| event | type | 携带字段 | 说明 |
|---|---|---|---|
| reasoning | `planner` | planner, label, note | 首帧：本轮由哪条路线规划，note 为降级原因 |
| reasoning | `status` | content | 阶段状态行 |
| reasoning | `plan` | content | 计划项（规则=固定模板；LLM=真实工具调用） |
| reasoning | `tool` | title, content | 工具返回结果 |
| reasoning | `reflection` | content | 反思结论（规则三态 / LLM 评审 / LLM 失败时规则兜底） |
| reasoning | `memory` | content | 长期记忆更新项 |
| reasoning | `metrics` | plan_latency_ms, total_tokens, llm_rounds, tool_rounds, history_turns, tool_count … | 尾帧：规划阶段真实开销 |
| answer | `token` | content | 回答 token |
| done | — | — | 流结束 |

---

## 6. 核心模块源码要点

### 6.1 agent_types.py — 共享契约（新增）

- `PLANNER_RULE="rule"` / `PLANNER_LLM="llm"` / `PLANNER_LABELS`：**路线标识与人类可读标签的唯一来源**，前端横幅直接渲染后端下发的 `label`，不硬编码。
- `AgentToolResult(name, title, content, metadata)`；`metadata["error"]=True` 标记工具失败。
- `AgentRun(mode, plan, tool_results, reflection, memory_updates, planner, fallback_reason, metrics)`
  —— **两条路线的唯一产出契约**，新增字段全部带默认值（既有 `tests/test_agent_reasoning.py` 无需改动）。
- `reasoning_events()` 形状是对外契约：**首个必为 `status`、末个必为 `reflection`**，tool 事件必含 `title`/`content`；新字段只追加不删改。
- `build_planner_event()` / `build_metrics_event()`：SSE 首尾帧构造，前端与评测共用。
- `extract_token_usage(message)`：**双通道兼容** `response_metadata["token_usage"]`（ChatTongyi）
  与 `usage_metadata`（LangChain 通用）；任何缺失返回全 0，绝不抛出。

### 6.2 tools.py — 共享工具层（新增）

- `ToolRegistry(rag_service, memory_store)` 是 4 个工具的**唯一实现**，对外两个视图：
  - `invoke(name, query, memory_items=None) -> AgentToolResult`（规则路线直接调用）
  - `as_langchain_tools() -> list[BaseTool]`（LLM 路线 `bind_tools`）
- **工具 description 决定 function calling 的选型准确率**，因此按「做什么 / 何时用 / 何时不用」
  三段式编写，不要压缩成一句话。
- `bind_memory_items()`：LangChain 工具视图的入参必须简洁（只有 `query`），长期记忆由调用方在执行前注入；
  未注入时回落到 `memory_store.load_recent()`。
- 各工具逻辑与重构前 `agent.py` 的 `_tool_*` **行为完全一致**（逐条平移，测试覆盖）。

### 6.3 agent_base.py — 基类与装配（新增）

- `LongTermMemory`：JSONL 追加写；`_extract_memory_candidates` 用关键词规则提取三类记忆
  （技术标签 / 待补强项 / 简历表达偏好）；加载时去重（最近 40 条比对）。**刻意不用 LLM/向量做记忆**。
- `BaseCareerAgent`：装配 `ToolRegistry` 与 `LongTermMemory`；提供 `compose_agent_context` / `finalize` /
  `load_history`；抽象方法 `prepare(query, session_uuid=None)`。
- `build_agent_service(rag_service, mode)`：按 planner 标识装配实例；**LLM 依赖缺失或初始化失败时自动回落规则路线**，保证接口层永远拿到可用实例。
- `rule_based_reflection(tool_results)`：确定性反思三态；规则路线直接使用，LLM 路线反思器失败时兜底，
  **两条路线降级行为一致**。

### 6.4 agent.py — 规则 Planner 路线（重构）

- 原 `CareerAgentService` 更名为 `RulePlannerAgent`，**保留 `CareerAgentService` 别名与
  `LongTermMemory` / `AgentRun` / `AgentToolResult` re-export**，外部引用不破。
- 设计取舍：求职场景只有 3 类固定意图，关键词路由即可零 token、可复现、延迟稳定。
  **代价是泛化性差**——复合意图只能命中单一分支，这是 A/B 评测中量化出的结构性短板。
- `_match_intent(text) -> str | None`：返回 `None` 表示未命中（区别于「综合求职分析」兜底值）。
- `_resolve_mode(query, turns)`：**当前问题优先**；命中不了才回看最近 3 轮用户发言推断意图
  （用于解析「那这个呢」这类省略式追问）。显式问题永远优先，因此不改变既有行为。
- `_detect_mode(query)` 保留旧语义（只看当前问题），供外部按原语义调用。
- `metrics` 中 `planner_llm_calls` / `total_tokens` 恒为 0，`history_turns` 记录实际轮数。

### 6.5 llm_agent.py — LLM Planner 路线（新增）

- LangGraph `StateGraph`，顺序执行、节点返回替换值（不使用 reducer）：
  `START → plan → (execute | reflect)`、`execute → plan`、`reflect → END`。
- `_route_after_plan`：还有待执行调用且 `executions < MAX_TOOL_ROUNDS` → 继续执行，否则反思。
  **`MAX_TOOL_ROUNDS` 说的是工具执行轮次**，规划器调用次数上限为 `+1`。
- `_plan_node`：`bind_tools(registry.as_langchain_tools())`；human 消息 =
  `_build_planning_input(query, history_digest)`；记录计划项（说明截断 60 字，防止越界输出污染计划列表）
  与 token 用量。
- `_execute_node`：
  - 工具名不在注册表 → 丢弃并计入 `invalid_tool_calls`（模型偶发返回空名也会被拦下）
  - **同工具同检索词 → 跳过执行**并计入 `duplicate_tool_calls`（prompt 已禁止重复调用，此处兜底省一次检索）
  - 工具抛错 → 产出 `metadata.error=True` 的结果，不中断流程
- `_reflect_node`：`ChatTongyi(REFLECTOR_MODEL)` 评审证据；异常或空输出 → 退回 `rule_based_reflection`。
- `prepare()` 降级规则（**任何降级都必须留痕，不允许静默**）：

  | 场景 | 处理 |
  |---|---|
  | 图执行异常/超时 | 回退规则路线，`fallback_reason` 记录原因 |
  | 全部工具调用均为幻觉（0 有效且 invalid>0） | 回退规则路线 |
  | **0 调用且无幻觉** | **视为模型主动判断「无需工具」的合法决策**，产出零工具 `AgentRun`，`no_tool_decision=1` |
  | 反思器失败 | 退回确定性反思文案 |
  | 回退 | **保留**本次尝试消耗的 token 与调用次数，保证账目真实 |

- `_derive_mode(tool_names)`：由实际选中的工具反推意图标签 —— LLM 模式下 `mode` 是决策的**结果**而非输入。

### 6.6 rag.py — LangChain RAG 链

- LCEL：`{context, input, history} | prompt | ChatTongyi(qwen-max) | StrOutputParser`。
- `RunnableWithMessageHistory` 挂 `get_history(session_id)` 读取 SQLite 历史。
- 检索上下文通过 `RunnableLambda(retrieve_context)` 在链内完成，内部 `print` 状态消息（供外层 SSE 捕获）。
- **两条 Planner 路线共用此链**，差异只在注入的 `enhanced_input`。

### 6.7 vector_stores.py — 混合检索

- `search_chroma`：embed_query → collection.query(n_results=k) → Document(filename, score=1-distance)。
- `get_retriever`：稠密（自定义 ChromaRetriever）+ 稀疏（BM25Retriever，k=3）；BM25 语料缺失则退化为纯稠密。
- **RRFRetriever**：`score += 1/(rank+60)` → 归一化（最高分=100%）→ 过滤 ≥97% → 最多 Top3。
- `hybrid_search_workflow` 返回 `(status_messages, results)`。

### 6.8 knowledge_base.py — 入库与材料管理

- 基于 `langchain_experimental.SemanticChunker`（DashScopeEmbeddings 计算句向量断点）。
- Chroma metadata 含 `filename / chunk_index / active / uploaded_at`；检索只召回 `active=True`
  （"切换材料"机制，startup 惰性迁移为旧 chunks 补 active）。
- 材料管理：`list_files()`（按文件聚合）、`toggle_file()`（启用/停用，替换式更新 metadata）、
  `delete_file()`（删 chunks + 清理 MD5 + 重建 BM25）。
- MD5 去重记录格式 `filename|md5`（兼容旧纯 md5 行）。

### 6.9 models.py + file_history_store.py — 存储与历史

- `User(last_cookie)` 存登录 uuid；`ChatSession(session_uuid)`；
  `ChatMessage(user_input / raw_output / output_uncode / code / streamline_input)`。
- 两个 relationship 均配置 `cascade="all, delete-orphan"`。
- `DatabaseChatMessageHistory`：**`add_messages` 为空实现**（pass）；历史写入完全依赖 `persist_message`；
  读取时旧消息用 `streamline_input`（60 字摘要）压缩上下文、最新一条用全文。
- 同步 engine 与异步 engine 并存（历史读取用同步 SQLAlchemy）。

### 6.10 api_service.py — 13 端点（详见 §7）

- 服务缓存：`_agent_cache: dict[planner_mode, BaseCareerAgent]`，两条路线各缓存一份实例，
  共享同一个 `RagService`（避免重复加载向量库）。
- `/chat` 为**结构化 SSE 事件流**（`text/event-stream`）：`sse_event()` 统一格式化事件帧；
  `reasoning` 与 `answer` 分离，`done` 收尾（见 §5.3）。
- **落库拆两步**：`persist_message`（`done` 前 await，毫秒级）+ `enrich_message`（`done` 后后台补写）。
- 登录：`sha256(password + SALT)`，`Set-Cookie: session_id`（httponly, samesite=lax, secure=False）。

### 6.11 evaluation/ — Planner A/B 评测（新增）

- `dataset.py`：20 条人工标注用例，字段 `{id, query, intent, expected_tools, note}`，覆盖
  岗位匹配 6 / 简历诊断 6 / 复合意图 4 / 历史语境 2 / 边界 2。
  **`expected_tools` 为空集合表示「正确行为是不调用任何工具」**（如纯寒暄）。
  `validate_dataset()` 校验 id 唯一、工具名在注册表内、query 非空。
- `evaluator.py`：
  - 纯函数指标 `required_recall` / `extra_calls` / `exact_match` / `percentile` /
    `aggregate` / `aggregate_by_intent` —— 无副作用，可离线单测。
  - `PlannerABEvaluator`：复用同一 `RagService` 与 Agent 实例，逐用例跑两条路线；
    **默认只测 `prepare()`（快、省 token）**，`--with-judge` 才调用裁判模型。
  - `report()` 产出 markdown（汇总对比 / 按意图分组 / 逐条明细 / 回退记录 / 结论口径）。

### 6.12 html/index.html — 前端

- 单文件 SPA：`loginScreen` / `appShell`（sidebar + workspace 双栏）。
- 三种模式：`upload` / `match` / `resume`，每种带快捷提问。
- **Planner 切换**：侧栏 `#sidePlanners`（`data-planner` 按钮）+ 顶栏 `#plannerBadge` + `#plannerHint` 说明文字；
  请求体携带 `planner_mode`。
- reasoning 面板：`planner` 事件渲染为**横幅**（规则路线用灰色样式，标注「无模型推理」），
  `metrics` 事件渲染为等宽小字一行；面板标题动态显示「Agent 工作流 · <label>」。
- 聊天用 `fetch` + `ReadableStream` 读 SSE，`bubble.textContent` 增量渲染。
- 前端预填测试账号 `candidate01 / career123`。

---

## 7. API 端点清单（13 个）

| 方法 | 路径 | 鉴权 | 说明 |
|------|------|------|------|
| GET | `/health` | 无 | 健康检查；**含 `planner`（当前默认路线）、`planner_modes`、`planner_notes`** |
| POST | `/auth/register` | 无 | 注册（query 参数传 username/password） |
| POST | `/auth/login` | 无 | 登录，Set-Cookie session_id |
| POST | `/sessions` | Cookie | 创建会话 |
| GET | `/sessions` | Cookie | 会话列表（按 update_time 倒序） |
| POST | `/chat` | Cookie | 发送消息，SSE 结构化流。<br>body: `session_uuid`, `input_text`, **`planner_mode`（可选，rule\|llm）** |
| GET | `/chat/{uuid}` | **无校验** | 获取某会话历史消息 |
| POST | `/knowledge/upload` | Cookie | multipart 上传 txt/md/pdf |
| GET | `/knowledge/files` | Cookie | 材料列表（按 filename 聚合：chunks / active / 上传时间） |
| POST | `/knowledge/files/toggle` | Cookie | 切换材料启用/停用（body: filename），并重建 BM25 |
| DELETE | `/knowledge/files?filename=` | Cookie | 删除材料（Chroma + MD5 清理 + 重建 BM25） |
| GET | `/agent/memory` | Cookie | 读取长期记忆（最近 12 条） |
| DELETE | `/delete/{uuid}` | Cookie + 归属校验 | 删除会话（401/403/404） |

> ⚠️ `GET /chat/{uuid}` **仍未校验会话归属**（甚至未要求登录），属安全隐患，见 §9.3。

---

## 8. 关键技术决策（面试/评审要点）

| 决策 | 动机 |
|------|------|
| **保留确定性 Planner 而非替换** | 3 类固定意图上规则路由是更优解（0 token、可复现、延迟低）；并存才能产出 A/B 数据。**取舍需要数据支撑而非直觉** |
| **LLM 路线用 function calling 而非 prompt 解析 JSON** | `bind_tools` 由模型侧保证结构化输出，比正则/JSON 解析稳；且工具名幻觉可被注册表拦下 |
| **两条路线共享 `ToolRegistry`** | 避免各写一份导致行为漂移；A/B 对比才有效 |
| **`AgentRun` 作为唯一契约** | 接口层与前端无需分支；新增字段带默认值保证向后兼容 |
| **历史压缩而非全量注入** | AI 回答动辄上千字，全量塞入会挤爆规划 prompt；3 轮 × 80 字足够解析指代 |
| **落库提前到 `done` 之前** | 消除"用户立刻追问读不到上一轮"的竞态（详见 §5.2） |
| **任何降级必须留痕** | `fallback_reason` + `metrics` 记录，禁止静默降级——否则 A/B 数据会失真 |
| **评测自带而非引入框架** | 避免引入 Ragas/DeepEval 等重依赖；指标纯函数化后可离线单测 |
| Chroma 替换 Milvus Lite | 免 Docker、纯 Python、Windows 友好；代价是大规模性能 |
| 混合检索 + RRF（k=60） | 向量擅语义、BM25 擅精确术语；RRF 只依赖排名，跨检索器可比，避免量纲/超参问题 |
| ≥97% 归一化过滤 | 以最高分为基准过滤弱相关证据，保 Top3 质量 |
| 记忆用 JSONL+规则而非向量 | 记忆需求结构化简单，避免过度设计 |
| 语义分块 SemanticChunker | 相比固定长度分块，chunk 语义完整，召回片段更有用 |
| 前后端同源免构建 | 原生 SPA + StaticFiles 挂载，一键启动演示 |

---

## 9. 已知问题与技术债（开发前必读）

### 9.1 ~~评估模块失效~~ ✅ **已解决（2026-09-10）**

原 `CareerLens-Agent-source/app/evaluation/evaluator.py` 调用已不存在的 `search_milvus`，无法运行。
本轮**未迁移旧模块**，而是重写了一套面向 **Planner A/B 对比**的评测：
`app/evaluation/{dataset,evaluator}.py` + `run_planner_eval.py`，已产出真实指标。
检索质量（Hit Rate / MRR）与生成质量（Faithfulness / Relevancy）的评估**仍未落地**，见 §10。

### 9.2 潜在 Bug / 健壮性问题

1. **Chroma id 冲突**：`doc_{existing_count+i}` 在删除重建/并发上传场景可能产生 id 偏移或冲突；
   多线程并发上传存在竞态。（**未修复**，见 §10 P1）
2. ~~`add_messages` 空实现导致"刷新丢失"~~ **部分缓解（2026-09-10）**：`add_messages` 仍为空实现，
   但落库已提前到 `done` 事件之前，历史在响应结束时即已可见。剩余风险：进程被强杀时最后一条可能丢失。
3. ~~SSE 状态转发 hack~~ ✅ 已于 2026-08-26 解决（协议升级为结构化事件流）。
4. **配置死参数**：`DENSE_WEIGHT=0.7 / SPARSE_WEIGHT=0.3`、`MAX_SPLIT_CHAR_NUMBER=1000`
   定义了但未使用（RRF 是排名融合、SemanticChunker 未设长度上限）。
5. **规划器无超时保护**：`ChatTongyi` 与 `dashscope.Generation.call` 均不支持透传 `timeout`，
   规划阶段的模型调用若挂起会一直阻塞 SSE 流（RagService 的 qwen-max 流式调用同样如此）。
   实施时曾定义 `PLANNER_LLM_TIMEOUT`，因无法优雅接上而**删除**，避免留死参数。
6. ~~前端状态不持久化~~ ✅ 已于 2026-08-26 解决。
7. **面试能力缺口**：模拟面试（interview_coach）在当前版已删除，前端无入口——与 README 描述不符。

### 9.3 安全

1. ~~`/delete/{uuid}` 无鉴权~~ ✅ 已解决；**`GET /chat/{uuid}` 仍有待补鉴权**（低风险，uuid 不可枚举）。
2. 密码用 `sha256+salt`（弱哈希，建议 bcrypt/argon2）；Cookie 无过期时间、`secure=False`。
3. `CORS allow_origins=["*"]` + `allow_credentials=True` 组合不安全（本地单机演示可接受，上线前必须收紧）。

### 9.4 依赖过重

`requirements.txt` 含 torch / transformers / sentence-transformers / FlagEmbedding / peft / accelerate
等大量与当前功能无关的重型包，安装体积大、耗时长。建议拆分为 `requirements-core.txt`。

### 9.5 规划能力边界（2026-09-10 实测）

1. `MAX_TOOL_ROUNDS` 默认 2，更长的多跳任务覆盖不足（成本约束下的取舍）。
2. 规划器只拿到**压缩摘要**（3 轮 × 80 字），更久远的上下文不可见。
3. 评测集 20 条，仅验证差异方向，**不具备统计显著性**。
4. 规划延迟包含两条路线共享的检索耗时（hybrid_retrieval 需调用 Embedding 接口），
   "规则路线 460ms" 并非纯路由开销。

---

## 10. 开发路线图建议（按优先级）

| 优先级 | 任务 | 状态 |
|--------|------|------|
| P0 | ~~修复并迁入评估模块~~ | ✅ **完成（2026-09-10）**，改为 Planner A/B 评测 |
| P0 | 鉴权与安全加固 | 部分完成；`GET /chat/{uuid}` 与 CORS/密码哈希待补 |
| P1 | 检索质量评估（Hit Rate / MRR）与生成质量评估（Faithfulness / Relevancy） | 待做 |
| P1 | Chroma id 生成重构（内容哈希替代自增计数） | 待做 |
| P1 | 恢复模拟面试（interview_coach 工具 + 前端第 4 模式） | 待做 |
| P1 | 多轮能力纳入 A/B 评测（当前评测仍为单轮） | 待做 |
| P2 | ~~LangGraph 重写 Agent~~ | ✅ **完成（2026-09-10）**，作为 `llm` 路线并存 |
| P2 | Reranker 二次排序（BGE / Qwen3-Reranker 对 RRF TopK 重排） | 待做 |
| P2 | 证据引用高亮（回答标注 `[来源: filename]`） | 待做 |
| P2 | 多格式支持（docx / 图片 OCR） | 待做 |
| P3 | 依赖精简（拆分 core 重型依赖） | 待做 |

---

## 11. 本地开发指南

### 11.1 环境与启动

```bash
# 1. 安装依赖（建议虚拟环境）
pip install -r requirements.txt

# 2. 配置 API Key（二选一）
#   方式A：项目根目录创建 .env
echo "DASHSCOPE_API_KEY=你的阿里云百炼密钥" > .env
#   方式B：环境变量
export DASHSCOPE_API_KEY="你的阿里云百炼密钥"

# 3. 启动服务（根目录执行）
python -m app.api.api_service
#    等价于 uvicorn app.api.api_service:app --host 0.0.0.0 --port 8000

# 4. 访问前端
#    http://localhost:8000/html/index.html
#    测试账号：candidate01 / career123（前端已预填）

# 5. 先灌入示例数据（可选，data/ 下 5 个文件，前端上传）
#    知识库为空时，Agent 反思会提示"证据不足，请补充材料"
```

> **本机环境提示**：项目实际使用的解释器为 `D:\Python\python.exe`（3.13.9）。
> `.idea/misc.xml` 中配置的 Python 3.14 环境未安装任何依赖，勿使用。

### 11.2 测试

```bash
python run_tests.py            # 全部 214 项（默认零网络，不消耗 token）
python run_tests.py -v         # 详细输出
```

- 单测一律用 `_FakeLLM` / `_FakeEmbeddings` / `_FakeRagService` 桩替换真实依赖，
  **测试不得消耗 token**。
- `tests/test_frontend_contract.py` 会解析 `html/index.html`，校验所有 `$("id")` 引用存在、
  SSE 字段契约一致、JS 语法（`node --check`），并守住「规则路线不得被表述成模型推理」这条红线。
  **改前端后务必跑它。**

### 11.3 Planner A/B 评测

```bash
python run_planner_eval.py                 # 全量 20 条，只测规划阶段（省 token）
python run_planner_eval.py --limit 5       # 只跑前 5 条（调试用）
python run_planner_eval.py --planner llm   # 只跑 LLM 路线
python run_planner_eval.py --with-judge    # 额外调用裁判模型评分（消耗额度）
```

报告默认写入 `docs/plans/2026-09-10-dual-planner-eval.md`。
全量跑约 3 分钟，消耗约 8 万 token（20 条 × 约 4k）。

### 11.4 切换 Planner 路线

- **运行时**：前端侧栏「规划器」切换，或请求体传 `planner_mode: "rule" | "llm"`。
- **全局默认**：`.env` 中 `PLANNER_MODE=rule|llm`。

### 11.5 关键配置项（`app/core/config_data.py`）

| 配置 | 默认 | 说明 |
|---|---|---|
| `PLANNER_MODE` | `rule` | 全局默认规划路线 |
| `LLM_PLANNER_MODEL` | `qwen-max` | 规划模型（需 function calling 能力） |
| `REFLECTOR_MODEL` | `qwen-turbo` | 反思模型 |
| `MAX_TOOL_ROUNDS` | `2` | **工具执行轮次**上限；规划器调用上限 = +1 |
| `PLANNER_HISTORY_TURNS` | `3` | 注入规划 prompt 的历史轮数 |
| `PLANNER_HISTORY_CHARS` | `80` | 历史每侧截断字数 |
| `JUDGE_MODEL` | `qwen-turbo` | 评测裁判模型 |
| `SIMILARITY_THRESHOLD` | `3` | 检索返回 TopK |

### 11.6 目录归属提醒

`database/`、`logs/`、`.workbuddy/` 为运行时/本地生成，已 gitignore；
改代码只动 `app/`、`html/`、`tests/`。三个源码副本中只有 `app/` 是活跃代码，
另两个为历史备份（source 含 Milvus 版评估与 Streamlit 上传器，可作参考）。

---

## 12. 附：模块 → 文件速查

| 想改什么 | 改哪里 |
|----------|--------|
| 新增 Agent 工具 | `app/core/tools.py`（`ToolRegistry` + `as_langchain_tools` + `TOOL_NAMES`） |
| 调整规划策略 / 兜底逻辑 | `app/core/llm_agent.py`（LLM 路线）、`app/core/agent.py`（规则路线） |
| 调整 Planner / Reflector 提示词 | `app/core/prompts.py` |
| 调整历史注入策略 | `app/core/agent_base.py`（`build_history_digest`）+ `config_data.py` |
| 新增 API 端点 / 改 SSE 事件 | `app/api/api_service.py` + `app/core/agent_types.py`（事件构造） |
| 新增评测用例 | `app/evaluation/dataset.py`（注意 `expected_tools=[]` 表示"不应调用"） |
| 新增评测指标 | `app/evaluation/evaluator.py`（写成纯函数便于单测） |
| 换模型 / 调参数 | `app/core/config_data.py` |
| 检索策略 | `app/core/vector_stores.py` |
| 入库 / 分块 | `app/core/knowledge_base.py` |
| 数据模型 | `app/models/models.py` |
| 前端页面 / Planner 切换 | `html/index.html`（改后跑 `test_frontend_contract`） |
| 共享类型与契约 | `app/core/agent_types.py`（改前先确认既有测试的契约断言） |
| 数据库结构变更 | `models.py` + 删除 `database/career_chat.db` 重建（MVP 无迁移工具） |
